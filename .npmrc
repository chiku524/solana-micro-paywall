# Configure npm to use legacy peer deps resolution
# This is needed because some packages (like @solana/wallet-adapter-wallets)
# haven't fully updated their peer dependencies for React 19 yet
# but they are compatible at runtime
legacy-peer-deps=true

